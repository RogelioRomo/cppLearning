#include <iostream>

int main() {
  // code
  std::cout << "Hello world!";
  return 0;
}

/*
 * Every C++ program must have exactly 1 main() function
 * It is the starting point of program execution
 * When execution hits the return statement, the program returns the integer
 * value to the operating system. If the return value is 0, then the program
 * terminates succesfully. If its not 0 then the operating system can check the
 * value returned and determine what went wrong
 * */
